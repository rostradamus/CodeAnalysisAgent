package src.transformer;

/**
 * TODO: Following Class should transform the class (except main class) as followings:
 *  - add import line to the class
 *  - iteratively call CustomMethodTransformer to transform each method within the class
 *  - Transform constructor to increment numObject of ClassTracker that has this classname
 *  - etc.
 */
public class CustomClassTransformer {
}
